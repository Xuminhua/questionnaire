[build]
  publish = "."
  command = ""

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"

# 部署设置
[context.production]
  publish = "."

[context.deploy-preview]
  publish = "."
